kind: ConfigMap
apiVersion: v1
metadata:
  # Must match the 'spring.application.name' property of the application
  name: ${project.artifactId}
data:
  application.properties: |
    # Placeholder to be overriden by contents of application.properties from materialzed profiles
